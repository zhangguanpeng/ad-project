import React from 'react';
import './style.scss';

interface IProps { }

interface IStates { }

class Footer extends React.Component<IProps, IStates> {
    state = {}

    render() {
        return (
            <div>这里是Footer组件</div>
        );
    }
}

export default Footer;
