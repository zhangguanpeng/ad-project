import React from 'react';
import './style.scss';

interface IProps { }

interface IStates { }

class ProductCard extends React.Component<IProps, IStates> {
    state = {}

    render() {
        return (
            <div>这里是ProductCard组件</div>
        );
    }
}

export default ProductCard;
