import React from 'react';
import './style.scss';

interface IProps { }

interface IStates { }

class Chart extends React.Component<IProps, IStates> {
    state = {}

    render() {
        return (
            <div>这里是Chart组件</div>
        );
    }
}

export default Chart;
